{
  "version": 2,
  "rootDirectory": "juris-ai",
  "framework": "nextjs",
  "installCommand": "cd .. && pnpm install --frozen-lockfile=false --ignore-scripts",
  "buildCommand": "pnpm run build",
  "outputDirectory": ".next"
}
